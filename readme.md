1.First create a Virtual Environment:
python -m venv venv

2.Go to venv folder then scripts then copy and paste the relative path of the activate.ps1 to the terminal to start venv

3.then use this command to install all the modules:
pip install -r requirements.txt
